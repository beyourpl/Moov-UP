# Déploiement auto : chaque push sur `main` → SSH vers le VPS → git pull + Docker.
#
# Secrets GitHub (Settings → Secrets and variables → Actions) :
#   VPS_HOST          ex. 178.104.253.218
#   VPS_USER          ex. root
#   VPS_SSH_KEY       clé privée OpenSSH complète (BEGIN … END). Le job « Préparer la clé SSH »
#                     écrit le fichier en UTF-8 (Python) pour éviter les soucis bash/multiligne + CRLF.
#   VPS_DEPLOY_PATH   ex. /root/tempo  (dossier qui contient docker-compose.yml)
#
# Sur le VPS : clone du repo une fois + backend/.env configuré.
# Repo public : git pull sans login. Repo privé : ajoute une deploy key (read) sur GitHub
# et configure-la sur le VPS (~/.ssh) pour ce dépôt.

name: Deploy to Hetzner

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-moovup
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Préparer la clé SSH sur le runner (UTF-8 + vérif ssh-keygen)
        env:
          SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          set -euo pipefail
          KEY_FILE="${GITHUB_WORKSPACE}/.ssh_ci/deploy.key"
          python3 << 'PY'
          import os
          import stat
          from pathlib import Path

          ws = Path(os.environ["GITHUB_WORKSPACE"])
          key_path = ws / ".ssh_ci" / "deploy.key"
          key_path.parent.mkdir(parents=True, exist_ok=True)
          raw = os.environ.get("SSH_KEY", "")
          if not raw or not raw.strip():
              raise SystemExit("VPS_SSH_KEY est vide — régénère le secret GitHub.")
          if raw.startswith("\ufeff"):
              raw = raw[1:]
          norm = raw.replace("\r\n", "\n").replace("\r", "\n")
          key_path.write_text(norm, encoding="utf-8", newline="\n")
          key_path.chmod(stat.S_IRUSR | stat.S_IWUSR)
          print("deploy.key bytes:", key_path.stat().st_size)
          PY
          ssh-keygen -y -f "$KEY_FILE" > /dev/null

      - name: SSH — git pull + Docker
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key_path: ${{ github.workspace }}/.ssh_ci/deploy.key
          debug: true
          timeout: 60s
          script_stop: true
          script: |
            set -ex
            cd "${{ secrets.VPS_DEPLOY_PATH }}"
            pwd
            git pull --ff-only
            docker compose up -d --force-recreate --build
