# CI : image seule (pas de bind-mount src/data qui masque l'image).
services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    env_file: ./backend/.env
    environment:
      RATE_LIMIT_ENABLED: "false"
    volumes:
      - sqlite_data:/app/db

volumes:
  sqlite_data:
