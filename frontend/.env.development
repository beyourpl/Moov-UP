# « npm run dev » démarre dev-api sur 8787 + Vite (front : http://localhost:3000 par défaut, voir vite.config.js).
# Backend Docker seul : docker compose up puis mets http://localhost:8000 et utilise « npm run dev:vite ».
VITE_API_URL=http://localhost:8787
