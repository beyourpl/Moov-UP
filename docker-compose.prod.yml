# Production VPS : pas de --reload, redémarrage auto des conteneurs.
services:
  backend:
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
  frontend:
    restart: unless-stopped
