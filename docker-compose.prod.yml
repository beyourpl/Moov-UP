# Production VPS : redémarrage auto, rate limit désactivé (stabilité derrière Caddy).
services:
  backend:
    environment:
      RATE_LIMIT_ENABLED: "false"
    restart: unless-stopped
  frontend:
    restart: unless-stopped
